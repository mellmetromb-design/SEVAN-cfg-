workflows:
  ios-workflow:
    name: iOS Build
    environment:
      flutter: 3.22.5
      xcode: latest
    scripts:
      - flutter pub get
      - flutter build ipa --release
    artifacts:
      - build/ios/ipa/*.ipa