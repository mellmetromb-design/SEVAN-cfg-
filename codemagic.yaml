workflows:
  ios-workflow:
    name: iOS Build
    environment:
      flutter: 3.19.6
    scripts:
      - flutter pub get
      - flutter build ipa --release
    artifacts:
      - build/ios/ipa/*.ipa